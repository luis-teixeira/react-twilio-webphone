import styled from 'styled-components';

const StyledCallLogsBody = styled.div`
  .title {
    border-bottom: 1px solid rgba(0, 0, 0, .2);
    padding-bottom: 15px;
  }
`;

export default StyledCallLogsBody;
